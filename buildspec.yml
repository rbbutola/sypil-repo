version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing dependencies
      - cd lambda
      - npm install  # only needed if you use dependencies
  build:
    commands:
      - echo "Zipping Lambda function"
      - zip -r ../lambda.zip .

artifacts:
  files:
    - lambda.zip
